import { ClockLoader } from "react-spinners";

const GlobalLoader = () => {
  return (
    <section className="min-h-screen flex items-center justify-center">
      <ClockLoader className="" />
    </section>
  );
};

export default GlobalLoader;
